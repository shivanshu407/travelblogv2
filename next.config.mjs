/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['fv1iqxumfd1pfiq9.public.blob.vercel-storage.com'],
    },
};

export default nextConfig;
